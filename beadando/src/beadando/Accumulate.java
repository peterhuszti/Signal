package beadando;

public class Accumulate<T, V> extends Action {
	
/*	private Accumulater<T, V> accumulater = null;
	
	public Accumulate(Accumulater<T, V> accumulater) {
		this.accumulater = accumulater;
		isConst = false;
	}
	
	public T act(T value, V old) {
		return accumulater.accumulate(old, value);
	}*/
}
