package beadando;

public class Map<RES, PAR> extends Action {
	
/*	private Mapper<RES, PAR> mapper;
	
	public Map(Mapper<RES, PAR> mapper) {
		this.mapper = mapper;
		isConst = false;
	}
	
	public RES act(PAR value) {
		return mapper.map(value);
	}
*/
}
